@inject DialogService DialogService

<header class="bg-purple w-screen  p-16">
    <div class="flex items-start justify-between mb-16">
        <div >
           <span class="bg-mygreen flex items-center justify-center rounded-full"> <h1 class="text-white font-roboto font-bold ">$ </h1> </span>
            <h1 class="text-white font-roboto font-bold ">dt money</h1>
        </div>

        <div class="flex items-center justify-center">
            <RadzenButton Text="Nova Transação" Click="@OpenTransactionDialog" />
        </div>
    </div>
</header>

<RadzenDialog />

@code {
    void OpenTransactionDialog()
    {
        DialogService.Open<NewTransaction>("Cadastrar Transação", new Dictionary<string, object>());
    }
}